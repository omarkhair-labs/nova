package com.nova.app.ui.theme

import androidx.compose.ui.unit.dp

/** Shared elevation roles. Prefer borders over shadows unless hierarchy needs lift. */
object NovaElevation {
    val flat = 0.dp
    val raised = 1.dp
    val floating = 6.dp
}
