package com.nova.app.ui.theme

/**
 * Shared motion duration roles. These are intentionally small and semantic;
 * individual features should select a role instead of inventing timings.
 */
object NovaMotion {
    const val fast = 120
    const val standard = 220
    const val emphasized = 320
}
